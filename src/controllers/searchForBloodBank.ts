import { Request,Response } from "express";
export function serchNearbyBloodBank(req:Request,res:Response) {
     try {
        const longitude=req.query.longitude
        const latitude=req.query.latitude
     } catch (error) {
        
     }
}